# June 2026

> Example month log. One dated bullet per shipped change, each ending with **Why**. Newest on top.
> The SessionStart hook surfaces this file so the next session reads what shipped + the reasoning.
> Replace these examples with your project's real entries.

- **2026-06-01** — Example: bootstrapped the Operating Soul in this repo — `.docs/principles/`,
  `.docs/common-issues/`, `.docs/recent-updates/`, the SessionStart + UserPromptSubmit hooks, and a
  root `CLAUDE.md` that mirrors the distilled principles. **Why:** so every session looks up prior
  learnings first and the project compounds knowledge instead of relearning each time.
- **2026-06-02** — Example: distilled a correction into a principle via `how-to-learn.md` (identify →
  why → pattern → check → write → place → commit) and added its index row. **Why:** a written
  principle with a trigger + expected behaviour lets a future session prove the learning stuck.
